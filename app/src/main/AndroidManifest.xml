<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.utadeo.librotec">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Chat"></activity>
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Mensajes"
            android:label="Mensajes" />
        <activity
            android:name=".MisLibros"
            android:label="Mis libros" />
        <activity
            android:name=".Libros"
            android:label="Busqueda" />
        <activity
            android:name=".Main2Activity"
            android:label="Principal" />
        <activity
            android:name=".Perfil"
            android:label="Nombre de usuario" />
        <activity
            android:name=".MainActivity"
            android:label="Librotec"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
    </application>

</manifest>